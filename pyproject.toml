[project]
name = "steganographyprobes"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = [
    "accelerate>=0.30",
    "matplotlib>=3.10",
    "numpy>=1.26",
    "peft>=0.10",
    "scikit-learn>=1.4",
    "torch>=2.0",
    "transformers>=4.40",
]
